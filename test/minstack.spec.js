import {strictEqual as equal} from 'assert';
import {MinStack} from '../src';

suite('MinStack', () => {
	suite('with Iterator', () => {
		test('size', () => {
			const s = new MinStack();
			s.push(1);
			s.push(2);
			s.push(3);
		});

		test('with iterator', () => {
		});

		test('min', () => {
		});
	});
});
