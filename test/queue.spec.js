import {strictEqual as equal} from 'assert';
import {Queue} from '../src';

suite('Queue', () => {
  test('without iterator', () => {
  });

  test('with iterator', () => {
  });

  test('peek', () => {
  });

  test('enqueue', () => {
  });

  test('dequeue', () => {
  });

  test('iterator', () => {
  });
});

